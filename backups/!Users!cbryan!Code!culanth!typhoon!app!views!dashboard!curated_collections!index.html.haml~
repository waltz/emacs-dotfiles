
%h2 Fieldsights CuratedCollections

%p.new= link_to "Create a new Fieldsights CuratedCollection", new_dashboard_curated_collection_path

- if @curated_collections.present?
  %table
    %tr
      %th ID
      %th Title
      %th User
      %th Created On
      %th Actions
    - @curated_collections.each do |i|
      %tr
        %td= i.id
        %td= i.title
        %td= i.user.email
        %td= i.created_at
        %td
          = link_to "View", fieldsight_path(i)
          = link_to "Edit", edit_dashboard_curated_collection_path(i)
          = link_to "Delete", dashboard_curated_collection_path(i), :method => :delete, :confirm => "Are you sure you want to delete this curated_collection?"
- else
  %strong No CuratedCollections, get rolling!
