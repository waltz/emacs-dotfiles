%h2 Commentaries

%p.new= link_to "Create a new Commentary", new_dashboard_commentary_path

- if @commentaries.present?
  %table
    %tr
      %th ID
      %th Title
      %th Created On
      %th Actions
    - @commentaries.each do |i|
      %tr
        %td= i.id
        %td= i.title
        %td= i.created_at
        %td
          = link_to "View", commentary_path(i)
          = link_to "Edit", edit_dashboard_commentary_path(i)
          = link_to "Delete", dashboard_commentary_path(i), :method => :delete, :confirm => "Are you sure you want to delete this Commentary?"
- else
  %strong No Commentaries yet; get rolling!
