
%h2 Curated Collection

%p.new= link_to "Create a new Curated Collection", new_dashboard_curated_collection_path

- if @curated_collections.present?
  %table
    %tr
      %th ID
      %th Title
      %th Created On
      %th Actions
    - @curated_collections.each do |i|
      %tr
        %td= i.id
        %td= i.title
        %td= i.created_at
        %td
          = link_to "View", curated_collection_path(i)
          = link_to "Edit", edit_dashboard_curated_collection_path(i)
          = link_to "Delete", dashboard_curated_collection_path(i), :method => :delete, :confirm => "Are you sure you want to delete this Curated Collection?"
- else
  %strong No Curated Collections yet; get rolling!
